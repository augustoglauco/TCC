// MVP: página stub, sem chamada à API de agendamentos ainda (ver docs/ROADMAP.md, Fase 7).
// Leitura apenas quando implementada — sem reagendar/cancelar pela UI (docs/FRONTEND.md §2).
export default function AgendamentosPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16">
      <h1 className="text-2xl font-semibold text-gray-900">Agendamentos</h1>
      <p className="mt-4 text-gray-600">
        Visualização de visitas agendadas via chat em construção.
      </p>
    </div>
  );
}
