# Resultado do teste local — suíte `mcp_b2b`

- Data: 2026-09-25 12:08
- Branch/commit: `claude/optimistic-gates-sk9uit` @ `8f1bfe4`
- Backend: http://localhost:8000 · log: `/tmp/tcc-backend.log`

Placar das verificações: PASSOU: 6

## 1. Checks automáticos

- `ruff check`: OK
- `pytest`: OK — 559 passed, 4 warnings in 83.04s (0:01:23)

## 2. Verificações do MCP B2B

- URL local: `http://127.0.0.1:8100/mcp` · URL pública: `https://augustoglauco.duckdns.org:8443/mcp`
- Parceiro usado no teste: `fornecedor-demo` (a chave não entra no relatório)
- Log do servidor: `/tmp/tcc-mcp-b2b.log`

### M1 — local, sem chave

**Esperado:** HTTP 401

**Obtido:** HTTP 401

**Veredito automático:** PASSOU

### M2 — local, chave errada

**Esperado:** HTTP 401

**Obtido:** HTTP 401

**Veredito automático:** PASSOU

### M3 — local, chave certa

**Esperado:** initialize + tools/list (4 ferramentas) + cotar ok; log mcp_b2b_ferramenta com o parceiro `fornecedor-demo`

**Obtido:** initialize ok (mcp-b2b); tools/list ok (4 ferramentas); tools/call cotar ok; cotação: {"itens": [{"produto_id": 1, "quantidade": 2, "preco_unitario": "24900.00", "percentual_desconto_aplicado": "0", "subtotal": "49800.00"}], "total": "49800.00"}; log com parceiro `fornecedor-demo` ✔

**Veredito automático:** PASSOU

### M4 — porta 8100 fechada para a rede

**Esperado:** conexão recusada pelo IP da LAN

**Obtido:** 192.168.1.200:8100 recusou a conexão

**Veredito automático:** PASSOU

### M5 — Caddy local com o domínio público (HTTPS)

**Esperado:** HTTP 401 com certificado válido

**Obtido:** HTTP 401

**Veredito automático:** PASSOU

### M6 — URL pública a partir desta rede

**Esperado:** HTTP 401 (roteador + DuckDNS + Caddy)

**Obtido:** HTTP 401

**Veredito automático:** PASSOU

## 3. Observações do testador

<!-- Opcional: o que você viu no chat do navegador, algo estranho na resposta, etc. -->
